"""Developer-side build, packaging and release tooling for ASH Captions.

Nothing under this package runs on an editor's machine. It runs on Ghazi's
build box to produce the bundle that `installer/install.ps1` unpacks, and to
publish it. See `docs/INSTALL.md` for the developer workflow and the release
manifest schema.
"""
